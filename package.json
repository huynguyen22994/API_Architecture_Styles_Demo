{
  "name": "api-architecture-styles-demo",
  "version": "1.0.0",
  "description": "Demo and implement gRPC, GraphQL and Rest in Microservices Architecture",
  "main": "index.js",
  "scripts": {
    "dev": "lerna run start:dev --stream --parallel",
    "build": "lerna run build --stream --parallel",
    "start:pm2": "pm2 start ecosystem.config.js",
    "kill:pm2": "pm2 delete all",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Huy Nguyen",
  "license": "ISC",
  "dependencies": {
    "autocannon": "^7.15.0",
    "readme-md-generator": "^1.0.0"
  },
  "devDependencies": {
    "lerna": "^8.0.0"
  }
}
